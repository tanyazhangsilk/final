# Vite 把 /api 代理到该地址（须与 uvicorn 端口一致）。C-Lodop 常占用 8000，后端请用 8001 或改此处。
VITE_DEV_PROXY_TARGET=http://127.0.0.1:8001

# 开发环境：长列表/报表类接口等待时间（毫秒）。勿加引号；改后需重启 npm run dev
VITE_API_TIMEOUT_MS=300000

# 表单提交/写操作等待时间（毫秒）。不设置时默认 20000
VITE_API_MUTATION_TIMEOUT_MS=20000
