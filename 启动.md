后端（推荐无热重载，避免监听整仓导致误重启、请求中断）：
cd D:\AAA-Echarge\echarge-master
.\.venv\Scripts\Activate.ps1
uvicorn main:app --host 127.0.0.1 --port 8001
# 若必须热重载，仅监听后端包目录（不要监听 frontend、node_modules、.venv）：
# uvicorn main:app --reload --host 127.0.0.1 --port 8000 --reload-dir app


管理端：
cd d:\AAA-Echarge\echarge-master\frontend
npm install
npm run dev

浏览器打开：
前端：http://127.0.0.1:5173 或 http://localhost:5173
后端健康检查：http://127.0.0.1:8001/api/v1/health（须与 `frontend/.env.development` 里 VITE_DEV_PROXY_TARGET、uvicorn 端口一致）
接口文档：http://127.0.0.1:8001/docs

说明：本机若装有 **C-Lodop**，其默认会占用 **8000**，浏览器走 Vite 代理仍指向 8000 时会拿到打印服务的 HTML，前端会表现为「电站选项加载失败」等。请后端固定 **8001**（如上），或改 `VITE_DEV_PROXY_TARGET` 与 uvicorn 为同一端口。

微信小程序：
用 微信开发者工具 打开项目：d:\AAA-Echarge\E-mini-main
详情 → 本地设置：勾选 不校验合法域名…
miniprogram\config\env.ts 里 BASE_URL 保持 http://127.0.0.1:8000/api/v1（真机改成电脑局域网 IP）。




























# E-Charge 联调启动说明

本文说明如何在本机同时启动 **后端（FastAPI）**、**管理端（Vue）** 与 **微信小程序**，三者通过同一套 API 与 MySQL 协同工作。

> 仓库根目录示例：`D:\AAA-Echarge`  
> 后端项目：`D:\AAA-Echarge\echarge-master`  
> 小程序项目：本目录 `E-mini-main`  
> 虚拟环境：若建在上一级，则为 `D:\AAA-Echarge\venv`（与 `echarge-master` 并列）

---

## 一、环境准备

1. **MySQL** 已安装并启动，已创建数据库（例如 `echarge`，与 `echarge-master\backend\.env` 中 `MYSQL_DB` 一致）。
2. **Node.js**（管理端 `npm run dev` 需要）。
3. **微信开发者工具**（调试小程序）。

---

## 二、启动后端（echarge-master）

在 PowerShell 中：

```powershell
# 进入后端目录
cd D:\AAA-Echarge\echarge-master

# 激活虚拟环境（路径按你本机实际 venv 位置调整）
D:\AAA-Echarge\venv\Scripts\Activate.ps1
# 若 venv 建在 echarge-master 内，则使用：
# .\venv\Scripts\Activate.ps1

# 若尚未安装依赖（清华源 403 时可改用官方源）
python -m pip install -i https://pypi.org/simple -r requirements.txt

# 初始化/更新演示数据（可重复执行；内含建表与「运营商入驻审核」演示种子）
python scripts\init_demo_data.py

# 若只补库表/缺列、不写业务演示数据，可单独执行：
# python scripts\patch_demo_schema.py

# 启动 API（本机调试推荐 127.0.0.1；默认不加 --reload，联调更稳）
uvicorn main:app --host 127.0.0.1 --port 8000
# 需要热重载时仅监听后端源码目录（勿用裸 --reload 监听整仓）：
# uvicorn main:app --reload --host 127.0.0.1 --port 8000 --reload-dir app
```

自检：浏览器打开 **http://127.0.0.1:8000/api/v1/health** ，应返回 `{"status":"ok"}` 等 JSON。

数据库连接配置见 **`echarge-master\backend\.env`**（`MYSQL_USER`、`MYSQL_PASSWORD`、`MYSQL_HOST`、`MYSQL_PORT`、`MYSQL_DB`）。

---

## 三、启动管理端（Vue，端口 5173）

新开一个终端：

```powershell
cd D:\AAA-Echarge\echarge-master\frontend
npm install
npm run dev
```

浏览器访问：**http://localhost:5173/**  

前端通过 Vite 将 **`/api`** 代理到 **`http://127.0.0.1:8000`**（`proxyTimeout` 已放宽），请保证上一步后端已在 **8000** 端口运行。若个别报表类接口仍超时，可在 `frontend/.env.development` 中设置 `VITE_API_TIMEOUT_MS=120000` 后重启 `npm run dev`。

---

## 四、启动微信小程序（E-mini-main）

1. 打开 **微信开发者工具** → 导入项目 → 目录选择 **`E-mini-main`**（含 `miniprogram`、`project.config.json` 的根目录）。
2. **详情 → 本地设置**：勾选 **「不校验合法域名、web-view（业务域名）、TLS 版本以及 HTTPS 证书」**（开发阶段访问 `http://127.0.0.1` 必需）。
3. 检查接口地址：**`miniprogram\config\env.ts`** 中的 `BASE_URL`  
   - 模拟器本机调试：`http://127.0.0.1:8000/api/v1`  
   - **真机预览**：改为电脑的 **局域网 IP**，例如 `http://192.168.1.x:8000/api/v1`，且后端需使用 `--host 0.0.0.0` 或可被局域网访问的监听方式。

演示登录（需已执行 `init_demo_data.py`）：

- 邮箱：`user@echarge.com`，密码：`123456`  
- 或手机：`13800138000`，密码：`123456`

---

## 五、推荐启动顺序

1. 启动 **MySQL**（库响应慢会拖慢所有带数据库的接口；工作台概览已做聚合优化）  
2. 启动 **后端**（`uvicorn`）  
3. 启动 **管理端**（`npm run dev`；`frontend/.env.development` 已默认拉长 `VITE_API_TIMEOUT_MS`）  
4. 用 **微信开发者工具** 打开并编译 **小程序**

---

## 六、常见问题

| 现象 | 处理 |
|------|------|
| `pip` 清华镜像 403 | 使用 `python -m pip install -i https://pypi.org/simple -r requirements.txt` |
| `WinError 10013` 绑定 8000 | 改用 `--host 127.0.0.1`，或换端口如 `8001` 并同步修改 `env.ts` 与前端代理 |
| 管理端「请求超时」 | ① 先开 **MySQL**，再开 **uvicorn(8000)**，`/api/v1/health` 能访问。② 使用 **`--reload`** 时保存代码会重启进程，**进行中的请求易超时**，可刷新页面或临时去掉 `--reload`。③ `frontend/.env.development` 中 `VITE_API_TIMEOUT_MS` **不要加引号**，改后需重启 `npm run dev`（默认已 300000ms）。④ 勿把 HTTP 服务错绑到 **3306**（MySQL 端口）。 |
| **运营商审核**列表为空 | 表 **`busi_operator_audit_applications`** 未建或为空：在后端 venv 下执行 **`python scripts\init_demo_data.py`**（会跑 `patch_demo_schema` 并写入 4 条演示申请）；或执行 **`python -m alembic upgrade head`**。首次打开审核页时，后端也会尝试 **自动建表并补种子**（需 DB 账号有建表权限）。新增真实申请可 **`POST /api/v1/admin/audit/operator-applications`**（管理员请求头与现有接口一致）。 |
| 小程序请求失败 | 检查 `BASE_URL`、开发者工具是否关闭域名校验、真机是否与电脑同网 |

更完整的后端说明见 **`echarge-master\README.md`**。
